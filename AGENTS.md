# 프로젝트 작업 지침

## 과제 목적과 기준

- 순수 HTML, CSS, JavaScript로 반응형 자기소개 포트폴리오 웹사이트를 만든다.
- 시각적 완성도보다 DOM 조작, 이벤트, 비동기 처리와 **이벤트 → 상태 변경 → 화면 업데이트** 흐름을 이해하고 설명하는 것을 우선한다.
- 요구사항 원문은 [과제 PDF](docs/assignment-requirements.pdf)다. 아래 내용이 누락되거나 불명확하면 원문을 확인한다.
- 아래 체크박스는 구현하고 검증한 뒤 완료로 표시한다. 선택 과제는 필수 기능 완료 후 검토한다.

## 작업 방식과 구조

- 설명과 작업 결과는 한국어로 작성한다.
- 한 번에 하나의 핵심 개념을 학습하는 작은 단위로 진행한다. 실제 코드의 위치, 선택 이유, 실행 흐름과 확인 방법을 설명하고 다음 개념은 사용자의 진행 의사에 맞춘다.
- 개념별 학습 기록은 `docs/learning/NNN-concept-name.md`에 한국어로 작성하고, `docs/learning/README.md`의 목록과 루트 README의 학습 기록 링크를 함께 갱신한다.
- 작업 단위가 끝나면 변경 내용과 필요한 검증 결과를 확인하고 해당 변경만 커밋한다. 커밋 메시지는 `유형: 한국어 설명` 형식으로 작성한다.
- 푸시는 사용자가 요청할 때만 한다.
- 단일 애플리케이션 소스는 `src/`에 둔다. `src/index.html`, `src/css/style.css`, `src/js/`, `src/images/`로 역할을 분리하되 필요한 시점에 디렉터리를 만든다.
- 테스트가 필요하면 `src/tests/`, 보조 스크립트는 `scripts/`, 작은 샘플은 `examples/`에 둔다.
- 문서는 `docs/`, 참고자료는 `docs/references/`, 실행 화면은 `docs/images/screenshots/`, 구조도는 `docs/images/diagrams/`에 둔다.
- 빌드 결과, 캐시, 임시 파일은 버전 관리하지 않는다.

## 학습 문서의 표현과 검증

- 개념, 코드에 적용한 내용, 실행 방법과 확인한 결과를 중심으로 작성한다. 사용자 학습 여부나 도구 환경의 제약을 설명하는 상투적인 문구는 넣지 않는다.
- 결과가 없는 항목은 실습 절차와 예상 동작으로 설명한다. 사용자의 경험이나 검증 성공을 만들어내지 않는다.
- 사용자만 확인할 수 있는 실습은 구체적인 절차와 확인 사항을 안내하고 받은 결과를 문서에 반영한다.
- 화면·실행 결과 확인에는 사용 가능한 도구를 활용하고, 필요하면 권한 범위 안에서 설치해 검증한다. 스크린샷은 실제 실행 화면을 캡처하고 기능과 확인 사항을 함께 기록한다.
- 정확성·재현성에 영향을 주는 미해결 문제는 사용자에게 알린다. 공개 문서에는 독자가 알아야 할 실제 기능 제약을 구체적으로 기록한다.
- 실제로 읽고 활용한 참고자료만 개념 설명과 구현의 근거로 기재한다.

## 필수 요구사항 체크리스트

### 1. 개발 환경과 HTML 구조 — PDF 2~3쪽

- [ ] VS Code와 Live Server로 개발 환경을 구성한다.
- [x] HTML, 외부 CSS, 외부 JavaScript, 이미지를 역할별로 분리하고 올바르게 연결한다. JavaScript에는 `defer`를 사용한다.
- [x] `header`, `nav`, `main`, `section`, `article`, `footer`를 의미에 맞게 사용한다.
- [x] Hero에 인사말과 CTA 버튼을 배치한다.
- [ ] About에 자기소개와 프로필 이미지를 배치한다.
- [x] Skills에 기술 스택 목록을 배치한다.
- [ ] Projects에 GitHub API로 가져온 저장소 카드를 표시한다.
- [x] Contact에 문의 폼을 배치한다.
- [x] Footer에 저작권과 소셜 링크를 배치한다.
- [x] 내비게이션에 각 섹션으로 이동하는 앵커 링크를 둔다.
- [x] 모든 이미지에 의미 있는 `alt`를 지정하고 폼의 `label`과 입력 요소를 `for`–`id`로 연결한다.

### 2. 스타일과 반응형 레이아웃 — PDF 3~4쪽

- [x] `css/style.css`를 사용하고 `:root`에 색상, 폰트, 간격 변수를 정의한다.
- [x] `[data-theme="dark"]`에 다크 모드 변수를 정의한다.
- [x] 내비게이션은 Flexbox로 로고를 왼쪽, 메뉴를 오른쪽에 배치한다.
- [x] Projects 카드는 Grid의 `auto-fit`, `minmax`로 반응형 배치한다.
- [x] 모바일 퍼스트로 작성하고 768px, 1024px 브레이크포인트를 사용한다.
- [x] 모바일에서 내비게이션 메뉴를 숨기고 햄버거 버튼을 표시한다.
- [ ] 버튼과 카드에 hover 효과와 transition을, 카드에 box-shadow를 적용한다.

### 3. JavaScript와 인터랙션 — PDF 4~5쪽

- [ ] `const`, `let`을 사용하고 `addEventListener`로 이벤트를 연결한다.
- [ ] `querySelector`, `querySelectorAll`, `textContent`, `innerHTML`, `classList.add/remove/toggle`을 활용한다.
- [ ] `click`, `submit`, `scroll`, `input` 이벤트를 다루고 필요한 곳에 `event.preventDefault()`를 사용한다.
- [x] 햄버거 버튼을 누를 때 `classList.toggle('active')`로 메뉴를 열고 닫는다.
- [x] 내비게이션 링크를 누르면 해당 섹션으로 부드럽게 이동한다.
- [ ] 스크롤 300px 이상에서 맨 위로 이동하는 버튼을 표시하고 클릭 시 맨 위로 이동한다.
- [ ] 스크롤 60px 이상에서 내비게이션 배경색을 변경한다.
- [ ] 다크 모드를 토글하고 로컬스토리지에 저장해 새로고침 후에도 유지한다.
- [ ] Intersection Observer로 스크롤 애니메이션을 구현한다. threshold는 0.2 이상을 권장한다.
- [ ] 스크롤 기준값과 Observer threshold를 README에 명시한다. 과제에서 이 값들은 변경을 허용한다.

### 4. 문의 폼 — PDF 5쪽

- [ ] 이름, 이메일, 메시지를 입력받는다.
- [ ] 필수값과 이메일 형식을 검증하고 오류를 해당 필드 근처에 표시한다.
- [ ] 제출 시 기본 동작을 막고 검증 통과 시 성공 메시지를 표시한다.
- 실제 이메일 전송은 선택 과제다. 전송을 구현하지 않았다면 실제로 전송했다고 안내하지 않는다.

### 5. ES6+, GitHub API와 상태 관리 — PDF 5~6쪽, 8쪽

- [ ] 화살표 함수, 템플릿 리터럴, 구조분해 할당을 활용한다.
- [ ] `map`으로 API 데이터를 HTML 카드로 변환하고 `forEach`로 배열을 순회한다.
- [ ] `fetch`와 `async/await`로 `https://api.github.com/users/{본인아이디}/repos`를 호출한다.
- [ ] 요청 중에는 스피너 또는 로딩 문구를 표시한다.
- [ ] 성공 시 저장소 카드 목록을 표시한다.
- [ ] 실패 시 오류 메시지와 재시도 버튼을 표시하고 `try/catch`로 오류를 처리한다.
- [ ] 저장소가 없으면 빈 상태 메시지를 표시한다.
- [ ] 레이트 리밋에 따른 403 응답도 오류 UI로 처리한다. PDF는 비인증 호출을 시간당 60회로 안내하므로 반복 요청에 유의한다.
- [ ] 테마, API 요청, 폼 검증 등 최소 세 가지의 상태 변경과 렌더링 흐름을 구현하고 설명할 수 있게 한다.

### 6. 검증과 제출 — PDF 6~7쪽

- [ ] 최신 Chrome에서 반응형 레이아웃, 모든 인터랙션, API 연동, 폼 검증을 확인한다.
- [ ] GitHub Pages로 배포하고 배포 URL에서 모든 필수 기능을 확인한다.
- [ ] 데스크톱, 모바일, 다크 모드 스크린샷을 저장한다.
- [ ] README에 프로젝트 설명, 사용 기술과 선택 이유, 실행·검증 방법, 배포 URL, 스크린샷과 주요 디렉터리 역할을 정리한다.
- [ ] README에 확인된 개발 기간, 개인·팀 여부, 본인 역할과 구현 범위, 결과와 한계를 기록한다. 확인되지 않은 기여나 성과는 만들지 않는다.
- [ ] README의 관련 문서와 이미지는 상대경로로 연결한다.
- [ ] GitHub 저장소 URL과 GitHub Pages 사이트 URL을 제출할 수 있도록 정리한다.

## 제약사항 — PDF 7쪽

- React, Vue, jQuery, Bootstrap, Tailwind CSS 등 외부 라이브러리·프레임워크를 사용하지 않는다.
- 아이콘(Font Awesome)과 웹 폰트(Google Fonts)는 허용한다.
- `var`, HTML의 `onclick`, 인라인 스타일(`style="..."`)을 사용하지 않는다.

## 선택 과제 — PDF 6~7쪽

- [ ] `filter`를 활용한 언어별 프로젝트 필터링
- [ ] Hero 타이핑 효과
- [ ] Formspree 또는 EmailJS를 이용한 실제 폼 전송
- [ ] `prefers-color-scheme`을 이용한 시스템 테마 감지

## 학습 확인 — PDF 2쪽

각 기능을 완성할 때 다음 항목을 자신의 코드로 설명할 수 있는지 확인한다.

- 시맨틱 태그를 선택한 기준
- Flexbox와 Grid의 차이와 각각의 사용 이유
- DOM 요소 선택과 이벤트 연결 흐름
- 화살표 함수, 구조분해 할당, 배열 메서드의 활용 이유
- 비동기 요청과 로딩·성공·실패 UI 처리
- 사용자 이벤트가 상태 변경과 화면 업데이트로 이어지는 과정

## 단계별 진행 순서

아래 항목은 전체 진행 순서이며 한 번의 대화에서 완료할 작업량을 뜻하지 않는다. 2단계부터 시맨틱 HTML, 앵커 링크, 외부 파일 연결처럼 개념별로 나누어 설명·확인한다.

1. 요구사항과 작업 기준 정리: 이 문서를 작성하고 PDF와 대조한다.
2. HTML 뼈대와 개발 환경: 여섯 섹션, 앵커 링크, 외부 파일 연결을 만들고 Live Server에서 확인한다.
3. 반응형 CSS: 모바일부터 시작해 태블릿·데스크톱 배치와 테마 변수를 구현한다.
4. 기본 인터랙션: 메뉴, 스크롤, 테마 저장과 애니메이션을 하나씩 구현한다.
5. 폼 검증: 입력 상태와 오류·성공 메시지를 연결한다.
6. GitHub API: 로딩·성공·에러·빈 상태와 재시도를 구현한다.
7. 최종 검증과 제출 준비: 기능을 점검하고 배포, 스크린샷, README를 완성한다.
