# 프로젝트 작업 지침

## 과제 목적

- 순수 HTML, CSS, JavaScript로 반응형 자기소개 포트폴리오 웹사이트를 만든다.
- 시각적 완성도보다 DOM 조작, 이벤트, 비동기 처리와 **이벤트 → 상태 변경 → 화면 업데이트** 흐름을 이해하고 설명하는 것을 우선한다.
- 작업 시작 시 `docs/progress.md`의 현재 상태와 이번 작업에 해당하는 체크리스트를 확인한다.
- 요구사항 체크리스트와 진행 상태는 [진행 상태](docs/progress.md)에서 관리한다. 요구사항 원문은 [과제 PDF](docs/assignment-requirements.pdf)이며, 체크리스트만으로 판단이 어려울 때 확인한다.

## 역할 분담

- **Codex**: 요구사항 해석, 학습 단계 설계, 개념 설명의 뼈대 작성, 구현·검증 결과·학습 문서 검토, 최종 판단, 커밋.
- **Claude Code**: 구현, 검증, 학습 문서 본문 작성, `docs/progress.md` 갱신.
- Codex는 위임할 때 목표, 범위, 제약, 완료 기준, 검증 방법을 지정한다. 학습 문서를 맡길 때는 다룰 개념, 관련 코드 위치, 설명할 순서를 뼈대로 전달한다.
- Claude Code는 결과를 `status` / `files` / `verification` / `open_issues` 고정 필드로 보고한다. `verification`에는 실행 명령·환경·실제 결과를, `open_issues`에는 실패와 미확인 항목을 포함한다.
- Codex는 `src/` 변경의 diff 전체와 핵심 동작의 검증 근거를 직접 확인한다. 모든 검증을 반복하지 않고 실패·불확실성·변경 위험이 있을 때 추가 검증한다.
- 문서는 새 개념 설명, 검증 결과, 완료 체크 변경을 반드시 읽고, 단순 목록·링크 갱신은 경로와 일치 여부를 확인한다.

## 작업 방식

- 설명과 작업 결과는 한국어로 작성한다.
- 한 번에 하나의 핵심 학습 목적을 다루는 작은 단위로 진행하고 다음 개념은 사용자의 진행 의사에 맞춘다. 같은 학습 목적에 필요한 구현만 묶는다. 예를 들어 Observer의 등장 효과는 함께 구현할 수 있지만 별도 버튼 hover는 분리한다.
- 개념 설명은 학습자료에 작성한다. 대화에서는 학습 개념을 별도로 강의하거나 문서 내용을 반복하지 않고, 작업 범위·변경 내용·검증 결과·자료 링크·남은 문제를 간결하게 전달한다.
- 사용자가 자료를 읽고 질문하면 해당 질문에 답하고, 이해가 어려웠던 설명과 예시를 기존 개념 문서에 보완한다. 질문만으로 다음 기능 구현을 시작하지 않는다.
- 이 프로젝트에서는 구현 후 대화에 개념·핵심 코드·실행 흐름 설명을 반드시 포함한다는 공통 지침 대신 위의 자료 중심 방식을 적용한다.
- 접근성 보완도 과제 요구사항에 포함된 범위에서 구현한다. 이미지의 `alt`와 폼의 `label` 연결 등 명시된 항목은 구현하고, 동작 줄이기 대응처럼 요구사항 밖의 보완은 추가하지 않는다.
- 작업 단위가 끝나면 변경 내용과 필요한 검증 결과를 확인하고 해당 변경만 커밋한다. 커밋 메시지는 `유형: 한국어 설명` 형식으로 작성한다.
- 작업 단위가 끝나면 `docs/progress.md`의 체크박스와 현재 상태를 함께 갱신한다.
- 푸시는 사용자가 요청할 때만 한다.

## 학습 기록

- 개념별 기록은 `docs/learning/NNN-concept-name.md`에 한국어로 작성하고, `docs/learning/README.md`의 목록과 루트 README의 학습 기록 링크를 함께 갱신한다.
- 작성 기준은 [학습 문서 작성 기준](docs/learning/GUIDE.md)을 따른다.

## 디렉터리 구조

- 단일 애플리케이션 소스는 `src/`에 둔다. `src/index.html`, `src/css/style.css`, `src/js/`, `src/images/`로 역할을 분리하되 필요한 시점에 디렉터리를 만든다.
- 테스트가 필요하면 `src/tests/`, 보조 스크립트는 `scripts/`, 작은 샘플은 `examples/`에 둔다.
- 문서는 `docs/`, 참고자료는 `docs/references/`, 실행 화면은 `docs/images/screenshots/`, 구조도는 `docs/images/diagrams/`에 둔다.
- 빌드 결과, 캐시, 임시 파일은 버전 관리하지 않는다.

## 제약사항 — PDF 7쪽

- React, Vue, jQuery, Bootstrap, Tailwind CSS 등 외부 라이브러리·프레임워크를 사용하지 않는다.
- 아이콘(Font Awesome)과 웹 폰트(Google Fonts)는 허용한다.
- `var`, HTML의 `onclick`, 인라인 스타일(`style="..."`)을 사용하지 않는다.
